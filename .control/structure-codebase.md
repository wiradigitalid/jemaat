---
type: structure
scope: codebase
verified: '2026-09-07'
commit: '32fef1e'
---

# Codebase Structure

## Verified

2026-09-07 at commit `32fef1e`, derived from the tree on disk — `git ls-files --cached --others
--exclude-standard` for what git can see, plus a filesystem listing of the root so ignored folders
are still named. `.gitignore` was honoured: `.temp/` is listed but not descended into.

## Top level

```text
jemaat/
├── .agent/                   # [tooling] stale skill kit, 49 skills, no AGENTS.md — see Unclaimed
├── .agents/                  # [tooling] instructions + 104 skills for Antigravity and Gemini
├── .claude/                  # [tooling] instructions + 104 skills and settings for Claude Code
├── .constitution/            # [docs] how we work — method/ is overwritten by update, project/ is ours
├── .control/                 # [docs] what currently holds: registries, decisions, questions, maps
├── .how/                     # [docs] how it is built — empty, born at G3
├── .how-rendered/            # [generated] reader's pages for the .how/ layer
├── .opencode/                # [tooling] 64 slash commands mirroring the skills
├── .temp/                    # git-ignored raw input data. Real church documents and personal data
├── .what/                    # [docs] what is promised — empty, born at G1
├── .what-rendered/           # [generated] reader's pages for the .what/ layer
├── .work/                    # [docs] scratch: the design canvas, the UI export, one styling spike
├── _bmad/                    # [tooling] BMad customisation under custom/*.toml
├── _bmad-output/             # [docs] skill working output — empty
├── docs/                     # [docs] agent-facing config seeded by wdi-method: domain, issue tracker
├── AGENTS.md                 # ★ every rule that binds an agent here; loaded every session
├── CLAUDE.md                 # one line: an @AGENTS.md include, so it can never drift
├── HANDOVER.md               # state of play written at the end of the design phase, for the next session
├── README.md                 # what this repo is
├── LICENSE                   # MIT, chosen so the platform stays forkable
├── .cursorrules              # the same rules again, for Cursor
├── .gitignore                # .temp/ and the usual local files
└── skills-lock.json          # pinned engine skills — to-spec, to-tickets, implement, tdd and two more
```

## Containers

Three deployable containers are registered at G3 (`built: true`):

### api

Backend REST API service in Go (`apps/api/`). Implements core domain logic, database migrations, and REST endpoints for all four Product Components. Connects to PostgreSQL (`db`, `built: false`).

### web

Web administration portal in React + Vite + TypeScript (`apps/web/`). Provides church office administration for membership records, roster planning matrix, care group oversight, and church settings.

### mobile

Mobile client application in Flutter (`apps/mobile/`). Provides congregational portal, 1-tap volunteer confirmations, offline-capable care group attendance check-in, and newcomer onboarding via QR code scanning.

## Libraries

None. Nothing in this repo is imported by anything else.

## Generated

| Path | Generated by |
| --- | --- |
| `.control/generated/*.md`, `*.yaml` | `.constitution/method/scripts/validate.py --generate` |
| `.what-rendered/`, `.how-rendered/` | the same run — the reader's pages, one per gate |
| `.control/structure-codebase.md`, `.control/structure-document.md` | `wdi-init` intent `structure` — this file included |
| `.work/design/*.dc.html`, `canvas.json` | `node .work/design/gen.mjs` from the `screens-*.mjs` modules |
| `.work/design/jemaat-mobile.html` | `node .work/design/pack.mjs` — the published canvas page |
| `.work/design/export/tokens.css` | `node .work/design/export/_gen-tokens.mjs` from `lib.mjs` |

## Unclaimed

- **`.agent/`** — 49 skills, no `AGENTS.md`, and nothing outside the agent kits references it.
  `.agents/` and `.claude/` each carry 104 skills after the 0.6.15 update, which touched only 13
  files in `.agent/`. It reads as a stale duplicate from an earlier installer generation, but the
  installer still writes into it, so deleting it is not this map's call.
- **`.temp/`** — its purpose is stated in `AGENTS.md`, so it is not unclaimed in the usual sense. It
  is named here because it is git-ignored and holds real personal data, and because a folder nobody
  documents in the map is the folder somebody misuses.

---

★ = key file: entry point, wiring root, the single place a rule is enforced, or a file that must be
opened before behaviour in its folder can be changed.
